<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Brandon Lederhouse %><br>
    <%= f.text_field :Brandon Lederhouse %>
  </div>
  <div class="field">
    <%= f.label :lederhou@grinnell.edu %><br>
    <%= f.text_field :lederhou@grinnell.edu %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
